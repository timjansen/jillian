/**
 * BoolType is a TypeDescriptor that allows only boolean values.
 */
native class BoolType extends TypeDescriptor:
  native constructor(onlyFullValues = false)
  
  override native checkType(value)

  override native convert(value)
  
  override native equals(other)
  
  override native serializeToString()
  
