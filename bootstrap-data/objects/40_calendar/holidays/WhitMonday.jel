Thing('WhitMonday', @HolidayCategory,
	properties={
    month: Range(5, 6),
    weekDay: @Monday,
    isMatch: (this, date)=>@Easter.isMatch(date - 50 @Day),
		matchesInYear: (this, year)=>@Easter.matchesInYear(year).map(d=>d+50 @Day)
  }
)
