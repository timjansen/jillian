Thing('WhitMonday', @HolidayCategory,
	properties={
    month: Range(5, 6),
    weekDay: @Sunday,
  	isMatch: (this, date)=>@Easter.isMatch(@Easter, date - 49 @Day),
		matchesInYear: (this, year)=>@Easter.matchesInYear(@Easter, year).map(d=>d+49 @Day)
  }
)
