/**
 * Helper for handling facts
 */
class FactHelper
  /**
   * Creates a Dictionary of root facts.
   */
  static createRootFacts(dict: any{}): Fact[]{} =>
    dict.map((name, value)=>[ValueFact(value, true, false, @Root, @BootstrapTranscript, 0, @Root, FactTypeEnum.Definition, 100)])
    
    
