Thing('Piece', @UnitOfMeasureCategory,
	facts=FactHelper.createRootFacts({
		quantityCategory: @Pieces,
		isPrimaryUnit: true,
		type: UnitTypeEnum.OTHER,
		convertsTo: {
			Dozen: {factor: Fraction(1, 12)}
		}
}))