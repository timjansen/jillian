/**
 * A MixinProperty is a property that can be shared between categories.
 */
native class MixinProperty extends DbEntry:
  native constructor(distinctName, typeDef: typedef)
  
  native type: TypeDescriptor

               
