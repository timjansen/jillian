/**
 * A MixinProperty is a property that can be shared between categories.
 */
native class MixinProperty extends NamedObject:
  native constructor(distinctName, type: typedef)   // TODO: a definition for typedefs should be used instead of any
               
               
